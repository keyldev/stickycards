<UserControl x:Class="helloworld.MVVM.View.ListCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:helloworld.MVVM.View"
             mc:Ignorable="d" Height="240" Width="300">
    <Border BorderThickness="0,2,0,0" CornerRadius="0,0,20,0" Margin="0,2,0,0" Background="#FF464646" BorderBrush="{Binding BackgroundColor}">
        <Grid>
           
            <Label HorizontalAlignment="Right" VerticalAlignment="Top" Content="11:35" Foreground="White"/>
            <Grid Margin="0,23,0,10">
                <ItemsControl ItemsSource="{Binding MyControlItems, Mode=TwoWay}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
